#!/usr/bin/env node

var app = require('../patternguide/node/app'),
    server = app.listen( 3000, function () {

      var host = server.address().address,
          port = server.address().port;

      console.log( "PatternGuide running at http://%s:%s", host, port );

    });
